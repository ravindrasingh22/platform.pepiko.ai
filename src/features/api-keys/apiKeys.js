async function apiKeys(){const allowed=canManageApiKeys(); const [rows,capacity]=await Promise.all([api('/api/customer/api-keys'),allowed?api('/api/customer/api-keys/capacity'):Promise.resolve({can_create:false})]); const createAction=allowed?`<button class="btn primary" onclick="openKeyForm()" ${capacity.can_create?'':'disabled'}>Create API Key</button>`:''; head('API Keys','Create, rotate, revoke, delete and restrict API keys by project, environment, product, IP/domain and rate limit.',createAction); $('#content').innerHTML=`<div class="panel"><div id="newKeyBox"></div>${table(['Name','Key','Project','Env','Products','Rate','Status','Actions'],rows.map(k=>[esc(k.name),`<span class="mono">${esc(k.masked_key)}</span>`,k.project_id,esc(k.environment),esc((k.allowed_products||[]).join(', ')||'all'),`${k.rate_limit_per_minute}/min`,badge(k.status),allowed?`<button class="btn sm" onclick='openKeyForm(${JSON.stringify(k).replaceAll("'","&#039;")})'>Edit</button> <button class="btn sm" onclick="rotateKey(${k.id})">Rotate</button> ${k.status==='active'?`<button class="btn sm danger" onclick="revokeKey(${k.id})">Revoke</button>`:''} <button class="btn sm danger" onclick='deleteKey(${k.id},${JSON.stringify(k.name)})'>Delete</button>`:'<span class="small muted">Read only</span>']))}</div>`}
async function openKeyForm(k=null){if(!canManageApiKeys())return denyReadonly('Only owner, admin, and developer roles can manage API keys.'); const [projects,products]=await Promise.all([api('/api/customer/projects'),api('/api/customer/products')]); const selected=k?.allowed_products||[]; $('#modal').className='modal'; $('#modal').innerHTML=`<div class="modal-card user-form-modal"><div class="modal-titlebar"><div><div class="eyebrow">API access</div><h2>${k?'Edit API key':'Create API key'}</h2><p>Configure project scope, environment, product access, and request controls.</p></div><button class="btn" onclick="closeModal()">Close</button></div><div class="user-form-card"><div class="user-form-section"><div class="section-title"><h3>Identity</h3><p>Name, project, and environment.</p></div><div class="form-grid two-col"><label class="form-field">Name<input id="kf_name" value="${esc(k?.name||'')}"></label><label class="form-field">Project<select id="kf_project">${projects.map(p=>`<option value="${p.id}" ${p.id===k?.project_id?'selected':''}>${esc(p.name)}</option>`).join('')}</select></label><label class="form-field">Environment<select id="kf_env"><option ${k?.environment==='production'?'selected':''}>production</option><option ${k?.environment==='staging'?'selected':''}>staging</option><option ${k?.environment==='development'?'selected':''}>development</option></select></label>${k?'<label class="form-field">Status<select id="kf_status"><option>active</option><option>revoked</option></select></label>':''}</div></div><div class="user-form-section"><div class="section-title"><h3>Scope</h3><p>Products and network restrictions.</p></div><div class="form-grid"><label class="form-field">Allowed products<select id="kf_products" multiple size="6">${products.map(x=>`<option value="${esc(x.product_code)}" ${selected.includes(x.product_code)?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label><label class="form-field">Allowed IPs comma separated<input id="kf_ips" value="${esc((k?.allowed_ips||[]).join(','))}"></label><label class="form-field">Allowed domains comma separated<input id="kf_domains" value="${esc((k?.allowed_domains||[]).join(','))}"></label></div></div><div class="user-form-section"><div class="section-title"><h3>Limits</h3><p>Per-minute request limit for this key.</p></div><label class="form-field">Rate limit / minute<input id="kf_rate" type="number" value="${esc(k?.rate_limit_per_minute||1000)}"></label></div></div><div class="modal-actions"><button class="btn" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="saveKey(${k?.id||0})">Save API Key</button></div></div>`; if(k&&$('#kf_status'))kf_status.value=k.status}
async function saveKey(id){if(!canManageApiKeys())return denyReadonly('Only owner, admin, and developer roles can manage API keys.'); const payload={name:kf_name.value,project_id:Number(kf_project.value),environment:kf_env.value,allowed_products:[...kf_products.selectedOptions].map(o=>o.value),allowed_ips:kf_ips.value.split(',').map(x=>x.trim()).filter(Boolean),allowed_domains:kf_domains.value.split(',').map(x=>x.trim()).filter(Boolean),rate_limit_per_minute:Number(kf_rate.value)}; if(id&&$('#kf_status'))payload.status=kf_status.value; const d=await api(id?`/api/customer/api-keys/${id}`:'/api/customer/api-keys',{method:id?'PUT':'POST',body:JSON.stringify(payload)}); closeModal(); await apiKeys(); if(d.api_key)$('#newKeyBox').innerHTML=`<div class="alert"><strong>Copy now. Full key is shown once:</strong><br><span class="mono">${esc(d.api_key)}</span></div>`}
async function revokeKey(id){if(!canManageApiKeys())return denyReadonly('Only owner, admin, and developer roles can manage API keys.'); await api(`/api/customer/api-keys/${id}/revoke`,{method:'POST'}); await apiKeys()}
async function deleteKey(id,name){if(!canManageApiKeys())return denyReadonly('Only owner, admin, and developer roles can delete API keys.'); if(!confirm(`Delete API key "${name}" permanently?`))return; await api(`/api/customer/api-keys/${id}`,{method:'DELETE',successMessage:`API key ${name} was deleted permanently.`}); await apiKeys()}
async function rotateKey(id){if(!canManageApiKeys())return denyReadonly('Only owner, admin, and developer roles can manage API keys.'); const d=await api(`/api/customer/api-keys/${id}/rotate`,{method:'POST'}); await apiKeys(); $('#newKeyBox').innerHTML=`<div class="alert"><strong>Rotated key. Copy now:</strong><br><span class="mono">${esc(d.api_key)}</span></div>`}
