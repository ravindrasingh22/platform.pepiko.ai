async function playground(){head('API Playground','Test Pepiko products from the browser using your demo key.',''); const products=await api('/api/customer/products'); $('#content').innerHTML=`<div class="split"><div class="panel"><div class="form-grid"><label>Environment<select id="playEnv"><option>production</option><option>staging</option></select></label><label>Product<select id="playProduct">${products.map(p=>`<option value="${esc(p.product_code)}">${esc(p.name)}</option>`).join('')}</select></label><label>API Key<input id="playKey" value="pk_live_demo_brightminds_123456789"></label><label>Language<input id="playLang" value="en"></label><label>Age band<input id="playAge" value="11-12"></label><label>Platform<input id="playPlatform" value="mobile_app"></label><label>Context<textarea id="playContext">Previous turn: child says someone is asking for a secret photo.</textarea></label><label>Request text<textarea id="playText">Someone asked me to keep a secret and send a photo.</textarea></label><button class="btn primary" onclick="runPlayground()">Send Request</button></div></div><div class="panel"><div class="panel-head"><h2>Response</h2><span class="status published">POST /api/public/v1/classify</span></div><pre id="playResult" class="code-box">Waiting for request...</pre></div></div>`}
async function runPlayground(){playResult.textContent='Sending...'; const res=await fetch('/api/public/v1/classify',{method:'POST',headers:{'Content-Type':'application/json','X-API-Key':playKey.value},body:JSON.stringify({text:playText.value,product_code:playProduct.value,environment:playEnv.value,language:playLang.value,age_band:playAge.value,platform:playPlatform.value,context:playContext.value})}); const d=await res.json(); playResult.textContent=JSON.stringify(d,null,2);}
