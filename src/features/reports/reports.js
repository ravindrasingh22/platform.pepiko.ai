async function reports(){head('Reports','Safety risk trends, top blocked categories, daily volume, latency and billing forecast.',''); const d=await api('/api/customer/reports'); $('#content').innerHTML=`<div class="split"><div class="panel"><h2>Risk trends</h2>${table(['Risk level','Count'],d.risk_trends.map(x=>[badge(x.risk_level),money(x.count)]))}<h2 style="margin-top:24px">Top categories</h2>${table(['Category','Count'],d.top_categories.map(x=>[esc(x.category),money(x.count)]))}</div><div class="panel"><h2>Daily API volume</h2>${table(['Date','Requests','Credits'],d.daily_volume.map(x=>[esc(x.date),money(x.requests),money(x.credits)]))}<div class="alert"><strong>Billing forecast:</strong> ${money(d.billing_forecast.estimated_month_end_credits)} estimated month-end credits.</div></div></div>`}
