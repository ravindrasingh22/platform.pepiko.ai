async function support(){head('Help / Support','Create tickets and track status updates managed by internal.pepiko.ai.','<button class="btn primary" onclick="openTicketForm()">Create Ticket</button>'); const rows=await api('/api/customer/support/tickets'); $('#content').innerHTML=`<div class="panel">${table(['Subject','Issue','Priority','Status','Progress','Last update','Actions'],rows.map(t=>[esc(t.subject),esc(t.issue_type),badge(t.priority),badge(t.status),`<div class="progress"><span style="width:${t.progress_percent}%"></span></div> ${t.progress_percent}%`,esc(t.last_customer_update||'-'),`<button class="btn sm" onclick="ticketDetail(${t.id})">View</button>`]))}</div>`}
function openTicketForm(){ $('#modal').className='modal'; $('#modal').innerHTML=`<div class="modal-card"><div class="panel-head"><h2>Create support ticket</h2><button class="btn" onclick="closeModal()">Close</button></div><div class="form-grid"><label>Subject<input id="tk_subject" value="Need API integration help"></label><label>Issue type<select id="tk_type"><option>general</option><option>billing</option><option>webhook</option><option>api_error</option><option>usage</option></select></label><label>Priority<select id="tk_priority"><option>normal</option><option>high</option><option>urgent</option></select></label><label>Message<textarea id="tk_message">Please help us with API setup.</textarea></label><button class="btn primary" onclick="createTicket()">Submit Ticket</button></div></div>`}
async function createTicket(){await api('/api/customer/support/tickets',{method:'POST',body:JSON.stringify({subject:tk_subject.value,message:tk_message.value,priority:tk_priority.value,issue_type:tk_type.value})}); closeModal(); await support()}
async function ticketDetail(id){const d=await api(`/api/customer/support/tickets/${id}`); $('#modal').className='modal'; $('#modal').innerHTML=`<div class="modal-card"><div class="panel-head"><h2>${esc(d.ticket.subject)}</h2><button class="btn" onclick="closeModal()">Close</button></div><p>${badge(d.ticket.status)} ${badge(d.ticket.priority)} Progress ${d.ticket.progress_percent}%</p><div class="progress"><span style="width:${d.ticket.progress_percent}%"></span></div><p class="muted">${esc(d.ticket.last_customer_update||'No update yet')}</p><h3>Conversation</h3>${table(['Author','Comment','Time'],d.comments.map(c=>[esc(c.author_email),esc(c.body),date(c.created_at)]))}<div class="form-grid"><label>Reply<textarea id="reply_body"></textarea></label><button class="btn primary" onclick="addTicketComment(${id})">Send Reply</button></div></div>`}
async function addTicketComment(id){await api(`/api/customer/support/tickets/${id}/comments`,{method:'POST',body:JSON.stringify({body:reply_body.value})}); closeModal(); await support()}
