async function dashboard(){head('Overview','Account health, usage, credits, plan and recent activity.','<button class="btn primary" onclick="showPage(\'playground\')">Test API</button>'); const d=await api('/api/customer/dashboard'); const w=d.wallet||{}; $('#content').innerHTML=`<div class="grid kpi-grid"><div class="kpi-card"><div class="kpi-icon">◉</div><div><div class="kpi-title">Credit Balance</div><div class="kpi-value">${money(w.balance_credits)}</div><div class="kpi-delta">Auto recharge ${w.auto_recharge_enabled?'enabled':'off'}</div></div></div><div class="kpi-card"><div class="kpi-icon">⇄</div><div><div class="kpi-title">Total API Requests</div><div class="kpi-value">${money(d.usage.total_requests)}</div><div class="kpi-delta">${d.usage.success_rate}% success</div></div></div><div class="kpi-card"><div class="kpi-icon">⏱</div><div><div class="kpi-title">Avg Latency</div><div class="kpi-value">${money(d.usage.avg_latency_ms)}ms</div><div class="kpi-delta">${d.usage.error_rate}% error rate</div></div></div><div class="kpi-card"><div class="kpi-icon">🔑</div><div><div class="kpi-title">Active API Keys</div><div class="kpi-value">${d.active_api_keys}</div><div class="kpi-delta">Plan: ${esc(d.organization.plan_code)}</div></div></div></div><div class="dashboard-layout"><div class="panel"><div class="panel-head"><h2>Usage by product</h2><button class="btn sm" onclick="showPage('usage')">View usage</button></div>${table(['Product','Requests','Credits'],(d.usage_by_product||[]).map(x=>[esc(x.product_code),money(x.requests),money(x.credits_used)]))}</div><div class="panel"><div class="panel-head"><h2>Current plan</h2><button class="btn sm" onclick="showPage('planLimits')">Plan & Limits</button></div><p class="muted">${esc(d.plan?.name||d.organization.plan_code)}</p><div class="progress"><span style="width:${Math.min(100,Math.round((d.usage.credits_used/(d.plan?.included_credits||250000))*100))}%"></span></div><p class="small muted">${money(d.usage.credits_used)} used of ${money(d.plan?.included_credits||0)} included credits.</p></div><div class="panel"><div class="panel-head"><h2>Recent activity</h2><button class="btn sm" onclick="showPage('audit')">Audit</button></div>${table(['Action','Actor','Time'],(d.recent_activity||[]).map(a=>[esc(a.action),esc(a.actor),date(a.created_at)]))}</div></div>`}
