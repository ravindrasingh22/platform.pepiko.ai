async function billing(){const allowed=canManageBilling(); head('Billing / Credits','Credit wallet, saved card billing, auto-recharge, postpaid records, invoices and product credit pricing.',allowed?'<button class="btn primary" onclick="openTopup()">Buy Credits</button><button class="btn" onclick="openPaymentMethod()">Add Card</button>':''); const d=await api('/api/customer/billing'); const w=d.wallet; $('#content').innerHTML=`<div class="grid kpi-grid"><div class="kpi-card"><div class="kpi-icon">◉</div><div><div class="kpi-title">Credit Balance</div><div class="kpi-value">${money(w.balance_credits)}</div><div class="kpi-delta">Mode: ${esc(d.organization.billing_mode)}</div></div></div><div class="kpi-card"><div class="kpi-icon">⇄</div><div><div class="kpi-title">Credits Used</div><div class="kpi-value">${money(d.usage.credits_used)}</div></div></div><div class="kpi-card"><div class="kpi-icon">💳</div><div><div class="kpi-title">Payment Methods</div><div class="kpi-value">${d.payment_methods.length}</div><div class="kpi-delta">Saved card billing</div></div></div><div class="kpi-card"><div class="kpi-icon">↻</div><div><div class="kpi-title">Auto Recharge</div><div class="kpi-value">${w.auto_recharge_enabled?'On':'Off'}</div><div class="kpi-delta">At ${money(w.auto_recharge_threshold)} credits</div></div></div></div><div class="split" style="margin-top:18px"><div class="panel" style="padding:0;overflow:hidden"><div class="modal-titlebar"><div><div class="eyebrow">Wallet automation</div><h2>Auto-recharge</h2><p>Automatically add credits when the wallet drops below the threshold.</p></div>${allowed?'<button class="btn sm" onclick="saveAutoRecharge()">Save</button>':'<span class="small muted">Read only</span>'}</div><div class="user-form-card"><div class="user-form-section"><div class="section-title"><h3>Settings</h3><p>Enable automation and set wallet thresholds.</p></div><div class="form-grid two-col"><label class="form-field">Enabled<select id="ar_enabled" ${allowed?'':'disabled'}><option value="true" ${w.auto_recharge_enabled?'selected':''}>true</option><option value="false" ${!w.auto_recharge_enabled?'selected':''}>false</option></select></label><label class="form-field">Threshold<input id="ar_threshold" type="number" value="${w.auto_recharge_threshold}" ${allowed?'':'disabled'}></label><label class="form-field">Recharge amount<input id="ar_amount" type="number" value="${w.auto_recharge_amount}" ${allowed?'':'disabled'}></label></div></div></div><div style="padding:0 22px 22px"><h2 style="margin-top:0">Product pricing</h2>${table(['Product','Base','Input/token','Output/token','Status'],d.product_pricing.map(p=>[esc(p.name),money(p.base_credits),money(p.input_token_rate),money(p.output_token_rate),badge(p.status)]))}</div></div><div class="panel"><h2>Payment methods</h2>${table(['Brand','Last4','Expiry','Status','Default'],d.payment_methods.map(m=>[esc(m.brand),m.last4,`${m.expiry_month}/${m.expiry_year}`,badge(m.status),m.is_default?'Yes':'No']))}<h2 style="margin-top:24px">Invoices</h2>${table(['Invoice','Mode','Amount','Credits','Status','Created'],d.invoices.map(i=>[esc(i.invoice_number),esc(i.billing_mode),`${esc(i.currency)} ${money(i.currency_amount)}`,money(i.credits),badge(i.status),date(i.created_at)]))}</div></div><div class="panel" style="margin-top:18px"><h2>Credit ledger</h2>${table(['Type','Delta','Balance After','Amount','Reason','Time'],d.ledger.map(l=>[esc(l.entry_type),money(l.credits_delta),money(l.balance_after),l.currency_amount?`${esc(l.currency)} ${money(l.currency_amount)}`:'-',esc(l.reason),date(l.created_at)]))}</div>`}
function openTopup(){if(!canManageBilling())return denyReadonly('Only owner, admin, and billing manager roles can manage billing.'); $('#modal').className='modal'; $('#modal').innerHTML=`<div class="modal-card user-form-modal"><div class="modal-titlebar"><div><div class="eyebrow">Wallet credits</div><h2>Buy credits</h2><p>Add prepaid credits to your workspace wallet.</p></div><button class="btn" onclick="closeModal()">Close</button></div><div class="user-form-card"><div class="user-form-section"><div class="section-title"><h3>Purchase</h3><p>Enter credits and the USD amount.</p></div><div class="form-grid two-col"><label class="form-field">Credits<input id="tu_credits" type="number" value="50000"></label><label class="form-field">Amount USD<input id="tu_amount" type="number" value="500"></label></div></div></div><div class="modal-actions"><button class="btn" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="topUp()">Buy Credits</button></div></div>`}
async function topUp(){if(!canManageBilling())return denyReadonly('Only owner, admin, and billing manager roles can manage billing.'); await api('/api/customer/billing/top-up',{method:'POST',body:JSON.stringify({credits:Number(tu_credits.value),currency_amount:Number(tu_amount.value),currency:'USD'})}); closeModal(); await billing()}
function openPaymentMethod(){if(!canManageBilling())return denyReadonly('Only owner, admin, and billing manager roles can manage billing.'); $('#modal').className='modal'; $('#modal').innerHTML=`<div class="modal-card user-form-modal"><div class="modal-titlebar"><div><div class="eyebrow">Payment method</div><h2>Add saved card</h2><p>Add a card for wallet purchases and billing operations.</p></div><button class="btn" onclick="closeModal()">Close</button></div><div class="user-form-card"><div class="user-form-section"><div class="section-title"><h3>Card</h3><p>Card brand and masked details.</p></div><div class="form-grid two-col"><label class="form-field">Brand<input id="pm_brand" value="Visa"></label><label class="form-field">Last4<input id="pm_last4" value="4242"></label><label class="form-field">Expiry month<input id="pm_m" type="number" value="12"></label><label class="form-field">Expiry year<input id="pm_y" type="number" value="2028"></label></div></div><div class="user-form-section"><div class="section-title"><h3>Billing</h3><p>Email used for billing receipts.</p></div><label class="form-field">Billing email<input id="pm_email" value="billing@brightminds.ai"></label></div></div><div class="modal-actions"><button class="btn" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="savePaymentMethod()">Save Card</button></div></div>`}
async function savePaymentMethod(){if(!canManageBilling())return denyReadonly('Only owner, admin, and billing manager roles can manage billing.'); await api('/api/customer/billing/payment-methods',{method:'POST',body:JSON.stringify({brand:pm_brand.value,last4:pm_last4.value,expiry_month:Number(pm_m.value),expiry_year:Number(pm_y.value),billing_email:pm_email.value})}); closeModal(); await billing()}
async function saveAutoRecharge(){if(!canManageBilling())return denyReadonly('Only owner, admin, and billing manager roles can manage billing.'); await api('/api/customer/billing/auto-recharge',{method:'PUT',body:JSON.stringify({enabled:ar_enabled.value==='true',threshold:Number(ar_threshold.value),amount:Number(ar_amount.value)})}); await billing()}
